# Activity
# Create a function that will loop through
# and print each number in the list and stops at the 48.
# In your function, the llist and the value
# that needs to be found should be passes as arguments.

numbers = [1,20,39,48,72,83]
value = 48

for numbers in 48:
    print(value)


def numbersThing(numbersList, value):
    for x in numbersList:
        print(X)
        if x == value:
            break

numbersThing(numbersList,value)

# create a function that will organize numbers from
# least to greatest

unorderList= [ 23, 600, 4, 91, 22, 49]

unorderList.sort(reverse= True)

print(unorderList)

# Organize list from least to greatest WITHOUT
# using sort()

